interface:
  display_name: "VetCoders Prune"
  short_description: "Prune repos down to living runtime"
  default_prompt: "Use $vetcoders-prune to map runtime-critical code, separate archive/tooling surface, and plan a safe cleanup."
