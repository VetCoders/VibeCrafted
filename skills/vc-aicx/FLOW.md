# vc-aicx Flow

1. Establish repo truth through `vc-init` and `Loctree:loctree` when work is repo-specific.
2. Retrieve intention history with `aicx` or `aicx-mcp`.
3. Verify recovered claims against live files, runtime, and current git state.
4. Report what is current, stale, missing, or contradicted.
